<%= form_with(model: catageroy) do |form| %>
  <% if catageroy.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(catageroy.errors.count, "error") %> prohibited this catageroy from being saved:</h2>

      <ul>
        <% catageroy.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
