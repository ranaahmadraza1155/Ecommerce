<%= form_for product, url:url do |form| %>
  <% if product.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
        <% product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title%>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :des %>
    <%= form.text_field :des %>
  </div>

  <div>
    <%= form.label :qty %>
    <%= form.number_field :qty %>
  </div>

  <div>
    <%= form.label :Price %>
    <%= form.number_field :price %>
  </div>

  <div>
  <%= form.select :Catageroy_id, Catageroy.all.map{|e| [ e.name, e.id]} %>
   
  </div>

  <div>
    <%= form.file_field :f_image %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
